//function expressions must go before function execution
const circle = function(radius)
{
        return Math.PI * radius * radius; 
}
//execute function below its expression

console.log('Area =', circle(3).toFixed(2)); //specify number of digits after the decimal point



function sideOfTriangle(){
    
}
